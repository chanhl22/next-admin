# Lib

이 폴더는 프로젝트의 유틸리티 함수, 상수, API 클라이언트 등을 포함합니다.

## 폴더 구조

- `utils/`: 범용 유틸리티 함수
- `api/`: API 관련 함수 및 설정
- `constants/`: 상수 정의
- `types/`: 공통 타입 정의

## 규칙

1. 순수 함수 위주로 작성
2. 비즈니스 로직은 최대한 이 곳에서 분리
3. 테스트 가능한 형태로 작성

## 사용 예시

- `utils/`: 범용 유틸리티 함수
- `api/`: API 관련 함수 및 설정